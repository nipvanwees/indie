import { BlockStyle } from "@prisma/client";

export const roundStyles: BlockStyle[] = [BlockStyle.ROUNDS];

export const timedStyles: BlockStyle[] = [BlockStyle.AMRAP];

export const hiddenStyles: BlockStyle[] = [BlockStyle.TOPDOWN, BlockStyle.ROUNDS];